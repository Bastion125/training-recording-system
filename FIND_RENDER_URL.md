# 🔍 Як знайти ваш Render URL

## Проблема

Якщо ви бачите помилку `400 Bad Request` або `xn--_service_name-v1k3a76a.onrender.com`, це означає, що в `api.js` не оновлено URL на реальний Render URL.

## ✅ Швидке рішення

### Крок 1: Знайдіть ваш Render URL

1. Відкрийте https://dashboard.render.com/
2. Увійдіть у свій акаунт
3. Знайдіть ваш **Web Service** (наприклад, `training-recording-backend`)
4. Натисніть на нього
5. У верхній частині сторінки ви побачите URL, наприклад:
   ```
   https://training-recording-backend.onrender.com
   ```
6. Скопіюйте цей URL

### Крок 2: Оновіть api.js

1. Відкрийте файл `frontend/app/scripts/api.js`
2. Знайдіть рядок:
   ```javascript
   const RENDER_API_URL = 'https://ВАШ_SERVICE_NAME.onrender.com/api';
   ```
3. Замініть `ВАШ_SERVICE_NAME` на назву вашого сервісу з Render
   
   **Приклад:** Якщо ваш URL `https://training-recording-backend.onrender.com`, то:
   ```javascript
   const RENDER_API_URL = 'https://training-recording-backend.onrender.com/api';
   ```

4. Збережіть файл

### Крок 3: Закомітьте та запуште

```bash
cd "/Users/a11/Desktop/Сайт/Навчання Альфа 1.1/ТЕСТ 2_1/github"
git add frontend/app/scripts/api.js
git commit -m "Оновлено Render API URL"
git push origin main
```

### Крок 4: Оновіть GitHub Pages

Після push зміни автоматично оновляться на GitHub Pages. Якщо ні:
1. Перейдіть до https://github.com/Bastion125/training-recording-system/settings/pages
2. Перевірте, що source встановлено на `main` branch
3. Якщо потрібно, натисніть **Save**

## 🔍 Альтернативний спосіб: Перевірка через Render Dashboard

Якщо ви не знаєте назву сервісу:

1. У Render Dashboard перейдіть до **Services**
2. Знайдіть ваш Web Service
3. Натисніть на нього
4. У розділі **Settings** → **Service Details** знайдіть **Service URL**
5. Скопіюйте URL

## ⚠️ Важливо

- URL має закінчуватися на `.onrender.com`
- Не забудьте додати `/api` в кінці в `api.js`
- Після оновлення перезавантажте сторінку на GitHub Pages (Ctrl+F5)

## 📝 Приклад правильного URL

**Неправильно:**
```javascript
const RENDER_API_URL = 'https://ВАШ_SERVICE_NAME.onrender.com/api';
```

**Правильно:**
```javascript
const RENDER_API_URL = 'https://training-recording-backend.onrender.com/api';
```

## ✅ Перевірка

Після оновлення перевірте:

1. Відкрийте консоль браузера (F12)
2. Перейдіть до Network tab
3. Спробуйте зареєструватися або увійти
4. Перевірте, який URL використовується для запитів
5. Він має бути ваш реальний Render URL, а не placeholder
