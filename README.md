# Training Recording System - CRM система з Базою знань

Сучасна веб-платформа для навчання, Бази знань та обліку навченості особового складу підрозділу БПС.

**Розробка за принципом TDD (Test-Driven Development)**

## 🎯 Основні можливості

### Особовий склад
- ✅ CRUD операції з особовим складом
- ✅ Прив'язка до user-акаунту
- ✅ Створення логіну/паролю адміністратором
- ✅ Прив'язка до екіпажів

### База знань / Навчання
- ✅ Ієрархія навчання: Курси → Модулі → Уроки
- ✅ Контроль послідовності проходження
- ✅ Правильний порядок відображення курсів
- ✅ Контроль доступу на основі ролей

### Аналітика та контроль
- Хто проходив курс
- Коли почав / коли завершив
- Скільки часу провів у курсі
- Які курси пройдені, які — ні

## 🏗️ Архітектура

```
training-recording-system/
 ├── backend/              # Backend API
 │   ├── src/
 │   │   ├── config/       # Конфігурація (Prisma)
 │   │   ├── controllers/  # Контролери
 │   │   ├── middleware/   # Middleware (auth)
 │   │   └── routes/       # Маршрути
 │   ├── prisma/           # Prisma схема та міграції
 │   ├── tests/            # Тести (Jest + Supertest)
 │   └── package.json
 ├── frontend/             # Frontend (існуючий)
 │   ├── app/
 │   │   ├── scripts/
 │   │   ├── styles/
 │   │   └── index.html
 ├── .gitignore
 └── README.md
```

## 📋 Технології

### Backend
- **Node.js** 18+
- **Express.js** - веб-фреймворк
- **Prisma** - ORM для MySQL
- **MySQL** - база даних (Railway)
- **JWT** - авторизація
- **Jest** + **Supertest** - тестування

### Frontend
- Vanilla JavaScript, HTML5, CSS3
- Існуючий код (не ламаємо API)

## 🚀 Швидкий старт

### 1. Клонування та встановлення

```bash
git clone https://github.com/Bastion125/training-recording-system.git
cd training-recording-system/backend
npm install
```

### 2. Налаштування бази даних

#### Локально (MySQL)

1. Створіть базу даних:
```sql
CREATE DATABASE training_recording CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. Створіть файл `.env` на основі `.env.example`:
```bash
cp .env.example .env
```

3. Відредагуйте `.env`:
```env
DATABASE_URL="mysql://root:password@localhost:3306/training_recording"
JWT_SECRET=your-secret-key
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000,http://localhost:8080
```

**Детальніше:** Див. `backend/.env.example`

#### Railway (Production)

1. Створіть проєкт на [Railway](https://railway.app)
2. Додайте MySQL сервіс
3. Скопіюйте `DATABASE_URL` з Railway
4. Додайте змінні середовища в Railway:
   - `DATABASE_URL` (автоматично)
   - `JWT_SECRET` (створіть надійний ключ)
   - `NODE_ENV=production`
   - `PORT` (Railway встановить автоматично)

### 3. Міграції бази даних

```bash
# Генерація Prisma Client
npm run prisma:generate

# Застосування міграцій
npm run prisma:migrate

# Або для production
npm run prisma:deploy
```

### 4. Запуск сервера

#### Development
```bash
npm run dev
```

#### Production
```bash
npm start
```

### 5. Запуск тестів

```bash
# Всі тести
npm test

# З покриттям
npm test -- --coverage

# Watch mode
npm run test:watch

# CI mode
npm run test:ci
```

## 🧪 Test-Driven Development (TDD)

Проєкт розробляється за принципом TDD:

1. **RED** - пишемо тест, який падає
2. **GREEN** - пишемо мінімальний код, щоб тест пройшов
3. **REFACTOR** - покращуємо код

### Структура тестів

```
backend/tests/
 ├── personnel.test.js    # Тести для CRUD personnel
 ├── courses.test.js      # Тести для курсів (порядок, доступ)
 └── auth.test.js         # Тести для авторизації
```

### Приклад запуску тестів

```bash
# Тести для personnel
npm test -- personnel.test.js

# Тести для курсів
npm test -- courses.test.js

# Тести для auth
npm test -- auth.test.js
```

## 📡 API Endpoints

### Авторизація
- `POST /api/auth/login` - Вхід
- `POST /api/auth/register` - Реєстрація
- `GET /api/auth/profile` - Профіль користувача

### Особовий склад
- `GET /api/personnel` - Список особового складу
- `GET /api/personnel/:id` - Один запис
- `POST /api/personnel` - Створити (Admin/SystemAdmin)
- `PUT /api/personnel/:id` - Оновити (Admin/SystemAdmin)
- `DELETE /api/personnel/:id` - Видалити (Admin/SystemAdmin)
- `POST /api/personnel/:id/create-account` - Створити акаунт (Admin/SystemAdmin)

### Курси
- `GET /api/courses` - Список курсів (з правильним порядком)
- `GET /api/courses/:id` - Один курс з модулями
- `POST /api/courses` - Створити (Admin/SystemAdmin/Readit)
- `PUT /api/courses/:id` - Оновити (Admin/SystemAdmin/Readit)

## 🔐 Ролі користувачів

- **SystemAdmin** - Повний доступ до всіх функцій
- **Admin** - Адміністративний доступ
- **Readit** - Інструктор (може створювати курси)
- **User** - Звичайний користувач (тільки перегляд)

## 📊 Структура бази даних

### Основні таблиці

- `roles` - Ролі користувачів
- `users` - Користувачі системи
- `personnel` - Особовий склад
- `crews` - Екіпажі
- `courses` - Курси
- `course_modules` - Модулі курсів
- `lessons` - Уроки
- `course_assignments` - Призначення курсів

### Зв'язки

- `users` ↔ `personnel` = 1-to-1
- `personnel` ↔ `crews` = many-to-1
- `personnel` ↔ `courses` = many-to-many (через `course_assignments`)

## 🚢 Деплой на Railway

### Швидкий старт

1. **Створіть проєкт на Railway**
   - Відкрийте [Railway Dashboard](https://railway.com/dashboard)
   - Створіть новий проєкт або виберіть існуючий

2. **Додайте MySQL Database**
   - New → Database → MySQL
   - Скопіюйте `DATABASE_URL`

3. **Додайте Backend Service**
   - New → GitHub Repo → `Bastion125/training-recording-system`
   - Settings → Root Directory: `backend`

4. **Налаштуйте Environment Variables**
   - `DATABASE_URL` (з MySQL service)
   - `JWT_SECRET` (надійний ключ мінімум 32 символи)
   - `NODE_ENV=production`
   - `CORS_ORIGIN=https://bastion125.github.io`

5. **Деплой**
   - Railway автоматично запустить деплой
   - Отримайте Railway URL з Settings → Networking

6. **Створіть адмін акаунт**
   - Connect → Open Shell → `npm run prisma:seed`
   - Облікові дані: `admin@test.local` / `Admin123!`

7. **Оновіть Frontend**
   - Відкрийте `frontend/app/scripts/api.js`
   - Замініть `REPLACE_WITH_RAILWAY_HOST` на ваш Railway URL

## 🧪 Запуск тестів перед деплоєм

```bash
# Локально
npm test

# CI/CD (GitHub Actions)
npm run test:ci
```

## 📝 Змінні середовища

### Обов'язкові
- `DATABASE_URL` - URL підключення до MySQL
- `JWT_SECRET` - Секретний ключ для JWT

### Опціональні
- `PORT` - Порт сервера (за замовчуванням 3000)
- `NODE_ENV` - Оточення (development/production)
- `CORS_ORIGIN` - Дозволені origin для CORS

## 🛠️ Розробка

### Створення нової міграції

```bash
npm run prisma:migrate
# Введіть назву міграції
```

### Перегляд бази даних

```bash
npm run prisma:studio
```

### Створення нового тесту

1. Створіть файл `tests/feature.test.js`
2. Напишіть тести (RED фаза)
3. Запустіть тести: `npm test -- feature.test.js`
4. Реалізуйте код (GREEN фаза)
5. Переконайтеся що всі тести проходять

## ✅ Критерії готовності (Definition of Done)

- ✅ Всі тести проходять
- ✅ Railway MySQL працює
- ✅ GitHub проєкт чистий (без build-артефактів, node_modules, .env)
- ✅ Особовий склад працює через UI + API
- ✅ Курси відображаються коректно (правильний порядок)
- ✅ Немає hardcode / secrets
- ✅ README містить всі необхідні інструкції

## 📞 Підтримка

Для питань та підтримки звертайтеся до адміністратора системи.

## 📝 Ліцензія

Всі права захищені © 2024
