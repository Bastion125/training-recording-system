# 🚀 Деплой на Render

## 📋 Передумови

- ✅ GitHub репозиторій: https://github.com/Bastion125/training-recording-system
- ✅ Render акаунт: https://dashboard.render.com/

## 🚀 Покрокова інструкція

### Крок 1: Підготовка коду

Переконайтеся, що всі зміни закомічені та запушені:

```bash
cd "/Users/a11/Desktop/Сайт/Навчання Альфа 1.1/ТЕСТ 2_1/github"

git add .
git commit -m "Налаштування для деплою на Render"
git push origin main
```

### Крок 2: Створення PostgreSQL бази даних на Render

1. Відкрийте https://dashboard.render.com/
2. Натисніть **New +** → **PostgreSQL**
3. Налаштування:
   - **Name:** `training-recording-db`
   - **Database:** `training_recording`
   - **User:** `training_user`
   - **Region:** Виберіть найближчий
   - **Plan:** Free (або інший за потреби)
4. Натисніть **Create Database**
5. Після створення перейдіть до **Connections** та скопіюйте:
   - **Internal Database URL** (для `DATABASE_URL`)
   - **Internal Database URL** (для `DIRECT_URL`)

### Крок 3: Створення Web Service на Render

1. У Render Dashboard натисніть **New +** → **Web Service**
2. Підключіть GitHub репозиторій:
   - Натисніть **Connect account** (якщо ще не підключено)
   - Виберіть репозиторій: `Bastion125/training-recording-system`
   - Натисніть **Connect**
3. Налаштування сервісу:
   - **Name:** `training-recording-backend`
   - **Region:** Виберіть той самий, що й для БД
   - **Branch:** `main`
   - **Root Directory:** `backend` ⚠️ **ВАЖЛИВО!**
   - **Runtime:** `Node`
   - **Build Command:** `npm install && npx prisma generate && npx prisma migrate deploy`
   - **Start Command:** `npm start`
   - **Plan:** Free (або інший за потреби)

### Крок 4: Налаштування змінних оточення ⚠️ КРИТИЧНО ВАЖЛИВО!

У розділі **Environment Variables** додайте **ОБОВ'ЯЗКОВО**:

**Обов'язкові змінні (мають бути встановлені ПЕРЕД деплоєм):**

1. **`DATABASE_URL`** - Internal Database URL з PostgreSQL сервісу
   - Перейдіть до PostgreSQL сервісу
   - У розділі **Connections** знайдіть **Internal Database URL**
   - Скопіюйте повний URL (виглядає як: `postgresql://user:password@host:port/database`)
   - Вставте в поле `DATABASE_URL`

2. **`DIRECT_URL`** - ⚠️ **ОБОВ'ЯЗКОВО!** Той самий URL що й DATABASE_URL
   - Скопіюйте той самий Internal Database URL
   - Вставте в поле `DIRECT_URL`
   - **Це критично важливо для Prisma міграцій!**

3. **`JWT_SECRET`** - згенеруйте через: `openssl rand -base64 32`
   - Або використайте будь-який випадковий рядок

4. **`NODE_ENV`** = `production`

5. **`PORT`** = `10000` (Render автоматично призначить, але можна вказати)

6. **`CORS_ORIGIN`** = `https://bastion125.github.io`

**⚠️ УВАГА:** Якщо `DIRECT_URL` не встановлено, деплой не вдасться з помилкою:
```
Error: Environment variable not found: DIRECT_URL
```

### Крок 5: Деплой

1. Натисніть **Create Web Service**
2. Render автоматично почне деплой
3. Процес деплою можна переглянути в **Logs**

### Крок 6: Перевірка деплою

Після успішного деплою:

1. **Отримайте URL сервісу:**
   - У Render Dashboard → Web Service
   - Скопіюйте URL (наприклад: `https://training-recording-backend.onrender.com`)

2. **Перевірте health checks:**
   ```bash
   curl https://ВАШ_URL.onrender.com/health
   curl https://ВАШ_URL.onrender.com/api/health
   curl https://ВАШ_URL.onrender.com/api/health/db
   ```

3. **Оновіть frontend URL:**
   - Відкрийте `frontend/app/scripts/api.js`
   - Оновіть `RAILWAY_API_URL` на ваш Render URL:
   ```javascript
   const RAILWAY_API_URL = 'https://ВАШ_URL.onrender.com/api';
   ```

## 🔧 Альтернативний спосіб: Використання render.yaml

Якщо ви хочете використати Infrastructure as Code:

1. Файл `render.yaml` вже створено в корені репозиторію
2. У Render Dashboard:
   - **New +** → **Blueprint**
   - Підключіть репозиторій
   - Render автоматично визначить `render.yaml`
   - Налаштуйте змінні оточення
   - Натисніть **Apply**

## 📝 Важливі примітки

### Root Directory
**Критично важливо:** Root Directory має бути `backend`, інакше Render не знайде `package.json`

### DATABASE_URL
- Використовуйте **Internal Database URL** для швидшого підключення
- External URL також працює, але повільніше

### Міграції
Міграції виконуються автоматично через `npx prisma migrate deploy` в Build Command

### Free Plan обмеження
- Сервіс "засинає" після 15 хвилин неактивності
- Перший запит після "сну" може займати до 50 секунд
- Для production рекомендовано використовувати платний план

## 🔍 Діагностика проблем

### Помилка: "Cannot find module"

**Рішення:**
- Перевірте Root Directory (має бути `backend`)
- Перевірте Build Command

### Помилка: "Database connection failed"

**Рішення:**
- Перевірте `DATABASE_URL` та `DIRECT_URL`
- Переконайтеся, що використовуєте Internal Database URL
- Перевірте, чи PostgreSQL сервіс запущений

### Помилка: "Prisma migrate failed"

**Рішення:**
- Перевірте `DIRECT_URL` (потрібен для міграцій)
- Перевірте логи деплою на детальні помилки

### Сервіс не відповідає

**Рішення:**
- На Free Plan сервіс "засинає" - це нормально
- Перший запит після сну може займати час
- Для production використовуйте платний план

## ✅ Чеклист деплою

- [ ] Код закомічений та запушений в GitHub
- [ ] PostgreSQL база даних створена на Render
- [ ] Web Service створено з правильним Root Directory (`backend`)
- [ ] Змінні оточення налаштовано (DATABASE_URL, DIRECT_URL, JWT_SECRET)
- [ ] Деплой завершено успішно
- [ ] Health checks працюють
- [ ] Frontend URL оновлено

## 🔗 Корисні посилання

- Render Dashboard: https://dashboard.render.com/
- GitHub репозиторій: https://github.com/Bastion125/training-recording-system
- Render документація: https://render.com/docs

## 📊 Перевірка статусу

Після деплою перевірте:

```bash
# Health check
curl https://ВАШ_URL.onrender.com/health

# API health
curl https://ВАШ_URL.onrender.com/api/health

# Database health
curl https://ВАШ_URL.onrender.com/api/health/db
```

## 🎉 Готово!

Після успішного деплою ваш backend буде доступний на Render, а кожен push до `main` автоматично запускає новий деплой.
